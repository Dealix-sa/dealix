"""Dealix ops_runtime package.

Defensive, dependency-light readers over the founder's private ops working
directory. Every function tolerates missing files and returns safe defaults so
mission-control / control-tower style reports can always render.
"""
