"""Dealix ops runtime — board-level operating system support modules.

This package provides the read-only inputs the board-level OS consumes
(CEO business score, finance roll-up). Prior layers in the Dealix
operating doctrine populate these modules with real evidence-backed
implementations. The stubs here keep the board-level pack runnable
when private_ops files are absent so the verifier and CI can still
exercise the scaffolding.
"""
