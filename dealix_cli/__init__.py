"""Dealix founder CLI — thin wrappers over private-ops files and operating docs.

This package is intentionally dependency-free (stdlib only) so it runs in any
environment where Python is available. Each command prints a checklist of
files to review — it never writes, sends, or charges anything.
"""

from __future__ import annotations

__all__ = ["__version__"]
__version__ = "0.1.0"
