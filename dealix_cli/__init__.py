"""Dealix CLI package.

Subcommands are implemented in prior operating-doctrine layers. The
board-level OS exposes its commands directly through the Makefile
(`make board`, `make strategic-decision`) so it remains runnable
independently of the CLI.
"""
