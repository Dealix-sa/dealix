"""Shared helper libraries for the ``scripts/`` CLI tools.

These modules back the quote-to-cash (V12), client-portal (V13) and
AI-router (V14) command-line tools. They are deliberately dependency-light
and deterministic so the tools run in CI without external services.
"""
