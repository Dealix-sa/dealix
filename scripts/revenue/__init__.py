"""Dealix revenue machine scripts package.

All scripts in this package are draft-only: they never send any message
externally. Every outreach artifact is written to disk for human review
before any manual send. A valid ``source_url`` is required for every
prospect — no source, no entry.
"""