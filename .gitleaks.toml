title = "Dealix gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "False positives — test fixtures, documentation examples, and ops scripts using env vars"
paths = [
    '^tests/',
    '^docs/',
    '^scripts/ops/',
    '^scripts/v7_launch_verify\.sh$',
    '^scripts/v10_master_verify\.sh$',
]
