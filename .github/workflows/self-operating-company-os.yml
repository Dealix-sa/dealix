name: self-operating-company-os

# Legacy/manual compatibility path only. The scheduled daily owner is
# governed-full-ops-daily.yml (issue #883).

on:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

concurrency:
  group: self-operating-company-os-${{ github.ref }}
  cancel-in-progress: false

jobs:
  run-self-operating-company-os:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run self-operating company OS in draft-only mode
        run: |
          python3 scripts/commercial/run_self_operating_company_os.py --mode draft-only --limit 50

      - name: Upload generated operating artifacts
        uses: actions/upload-artifact@v7
        with:
          name: self-operating-company-os-report
          path: reports/self_operating_company_os/
          if-no-files-found: error
