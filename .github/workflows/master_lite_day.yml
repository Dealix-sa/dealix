name: Master Lite Day

on:
  workflow_dispatch:
  schedule:
    - cron: '20 6 * * *'

permissions:
  contents: read
  actions: read

jobs:
  master-lite-day:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install minimal Python deps
        run: python -m pip install pyyaml

      - name: Run Master Lite Day
        env:
          GOOGLE_SEARCH_API_KEY: ${{ secrets.GOOGLE_SEARCH_API_KEY }}
          GOOGLE_SEARCH_CX: ${{ secrets.GOOGLE_SEARCH_CX }}
          GOOGLE_MAPS_API_KEY: ${{ secrets.GOOGLE_MAPS_API_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          APP_SECRET_KEY: ${{ secrets.APP_SECRET_KEY }}
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
          API_KEYS: ${{ secrets.API_KEYS }}
          ADMIN_API_KEYS: ${{ secrets.ADMIN_API_KEYS }}
          DEALIX_ADMIN_API_KEY: ${{ secrets.DEALIX_ADMIN_API_KEY }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
        run: ./scripts/dealix_master_lite_day.sh

      - name: Upload Master Lite artifacts
        uses: actions/upload-artifact@v4
        with:
          name: master-lite-day
          path: |
            company/reports/
            company/outbox/
            company/crm/
            company/lead_research/
            founder_os/output/
