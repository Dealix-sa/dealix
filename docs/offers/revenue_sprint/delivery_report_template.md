# Delivery Report — `<client>` — Revenue Sprint

> Day 7 deliverable. Public template; live report stored in
> `clients/<client>/delivery_report.md` in the private repo.

## What we agreed

One paragraph, copy/pasted from `client_intake.md`.

## What we delivered

3 named artefacts, each linked to its file:

1. `<artefact 1>` — `<path>`
2. `<artefact 2>` — `<path>`
3. `<artefact 3>` — `<path>`

## Number we hit (or missed)

- Target from intake:
- Actual:
- Variance and reason:

## Evidence

- Screenshots / files: `<paths>`
- Data sources used: `<list>`
- Verifier scripts that passed against the delivery: `<list>`

## What surprised us

One bullet — observation about the client's data / market we did not expect.
This is the "intelligence transfer" the client paid for.

## What you should do next (your side)

3 specific actions the client should take in the first 14 days post-delivery.

## What we can do next (our side)

The retainer ask — link to `retainer_ask.md`.

## Feedback request

Single ask — link to `feedback_request.md`.

## Acceptance

- [ ] Client confirmed receipt.
- [ ] Client signed off on the artefacts (date: ____).
- [ ] Final invoice issued / payment cleared.
- [ ] Sprint marked complete in `pipeline/pipeline_tracker.csv` (stage = `delivered`).
