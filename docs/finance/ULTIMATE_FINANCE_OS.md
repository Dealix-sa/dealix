# Ultimate Finance OS

`finance/cash_collected.csv` and `finance/payment_capture_queue.csv`
back the `/finance` page. The `finance_copilot` agent summarizes
totals and surfaces payment follow-ups due.

This view is a heuristic snapshot; the source of truth for accounting
remains the existing finance OS endpoints.
