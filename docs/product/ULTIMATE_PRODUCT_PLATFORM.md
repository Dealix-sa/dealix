# Ultimate Product Platform

The product platform is the productization loop:

```
repeating manual flow → nominated candidate → tested → SLA →
priced offer → catalog entry → retainer eligibility
```

`product/productization_candidates.csv` is the queue of nominated
candidates. Surfaced on `/product`. Driven by the `productization_agent`.
