# Open Design-inspired Agent Usage

Use the Dealix Design Command Room skill for UI/deck/page/proof-pack work.

Do not ask the agent to copy Open Design. Ask it to apply the pattern:

```text
brief -> design contract -> draft artifact -> critique -> approval -> handoff
```
