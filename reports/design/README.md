# Dealix Design Reports

This directory is reserved for reviewed design artifacts generated through the Dealix Design Command Room OS.

Generated artifacts should include metadata, approval state, safety status, and handoff target before they are used for sales, client delivery, or production UI.

Default artifact state is `draft` or `needs_review`.
