# Launch Reports

Generated by:

```bash
python scripts/launch/run_launch_now.py
```

Expected generated files:

- `latest.md` — founder-readable launch verdict.
- `latest.json` — machine-readable gate results.

This directory should contain reports only. A failed launch report is useful because it tells the founder which gate blocks public launch.
