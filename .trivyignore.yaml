# Trivy ignore file — KNOWN, TEMPORARY exceptions only.
#
# These are real, pre-existing Next.js vulnerabilities in the `next@15.1.3`
# dependency of both frontend apps. They are NOT false positives. They are
# tracked for a dedicated Next.js upgrade PR (target: next@15.5.16, which
# clears all of the entries below). Until that lands, they are suppressed here
# ONLY for the two affected lockfiles, with an expiry so they cannot be
# forgotten — after `expiredAt` Trivy fails again and forces the upgrade.
#
# Do NOT add unrelated entries here. New CRITICAL/HIGH findings must block CI.

vulnerabilities:
  # CRITICAL — Authorization Bypass in Next.js Middleware
  - id: CVE-2025-29927
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.2.3+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # CRITICAL — RSC pre-authentication RCE via unsafe deserialization
  - id: CVE-2025-55182
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.1.9/15.5.7+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — Next.js denial of service
  - id: CVE-2025-49826
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.1.8+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — Information disclosure via middleware bypass (Pages Router)
  - id: CVE-2026-44573
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.5.16+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — SSRF via crafted WebSocket upgrade requests
  - id: CVE-2026-44578
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.5.16+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — DoS via crafted POST requests to server actions
  - id: CVE-2026-44579
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.5.16+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — DoS with Server Components
  - id: GHSA-8h8q-6873-q5fj
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — HTTP request deserialization DoS (insecure React)
  - id: GHSA-h25m-26qc-wcjf
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.5.10+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — DoS with Server Components
  - id: GHSA-mwv6-3258-q52c
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.5.8+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
  # HIGH — DoS with Server Components
  - id: GHSA-q4gf-8mx6-v5v3
    paths:
      - "frontend/package-lock.json"
      - "apps/web/package-lock.json"
    statement: "next@15.1.3; fixed in 15.5.15+. Tracked for dedicated upgrade PR."
    expiredAt: 2026-09-06
