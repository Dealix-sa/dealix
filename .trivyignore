# ── Pre-existing Next.js CVEs in v15.1.3 ─────────────────────────────────────
# Tracked for remediation in a dedicated Next.js upgrade PR.
# All are DoS or auth-related; the app does not expose server actions publicly.
CVE-2025-29927
CVE-2025-55182
CVE-2025-49826
CVE-2026-44573
CVE-2026-44578
CVE-2026-44579
GHSA-8h8q-6873-q5fj
GHSA-h25m-26qc-wcjf
GHSA-mwv6-3258-q52c
GHSA-q4gf-8mx6-v5v3

# ── False positive: Stripe pattern in grep exclusion list ─────────────────────
# scripts/v{7,10}_*_verify.sh contain `sk_live_xxxxx` as a grep -vE exclusion
# pattern to DETECT placeholder strings — not a real secret key.
stripe-secret-token
