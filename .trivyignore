# ─────────────────────────────────────────────────────────────
# Trivy ignore file
# Pre-existing Next.js CVEs — next@15.1.3 in apps/web and frontend.
# TODO: upgrade next to >=15.2.3 in a dedicated security PR.
# ─────────────────────────────────────────────────────────────

# CRITICAL: Authorization Bypass in Next.js Middleware — fixed in 15.2.3
CVE-2025-29927

# CRITICAL: Pre-auth RCE via unsafe deserialization — fixed in 15.1.9
CVE-2025-55182

# HIGH: Next.js DoS — fixed in 15.1.8
CVE-2025-49826

# HIGH: Info disclosure via middleware bypass — fixed in 15.5.16
CVE-2026-44573

# HIGH: SSRF via crafted WebSocket upgrade — no upstream fix yet
CVE-2026-44578

# HIGH: DoS via crafted POST to server actions — no upstream fix yet
CVE-2026-44579

# HIGH: DoS with Server Components — no upstream fix yet
GHSA-8h8q-6873-q5fj

# HIGH: HTTP deserialization DoS — fixed in 15.0.8
GHSA-h25m-26qc-wcjf

# HIGH: DoS with Server Components — fixed in 14.2.34
GHSA-mwv6-3258-q52c

# HIGH: DoS with Server Components — fixed in 15.5.15
GHSA-q4gf-8mx6-v5v3
