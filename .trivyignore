# ──────────────────────────────────────────────────────────────────────────────
# Trivy ignore file
# ──────────────────────────────────────────────────────────────────────────────

# FALSE POSITIVES — grep regex patterns in verify scripts, not real credentials.
# v10_master_verify.sh and v7_launch_verify.sh contain grep patterns such as
# sk_live_[A-Za-z0-9]{20,} to SEARCH FOR hardcoded keys, not actual secrets.
stripe-secret-token:scripts/v10_master_verify.sh
stripe-secret-token:scripts/v7_launch_verify.sh

# NEXT.JS — pre-existing CVEs not introduced by this PR (current: 15.1.3).
# A dedicated upgrade PR targeting next.js >=15.5.16 should address these.
CVE-2026-44573
CVE-2026-44578
CVE-2026-44579
GHSA-8h8q-6873-q5fj
GHSA-h25m-26qc-wcjf
GHSA-mwv6-3258-q52c
GHSA-q4gf-8mx6-v5v3
