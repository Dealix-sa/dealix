# Dealix Brand Source

Drop master brand source files here:

- `dealix-brand-board.jpeg` — current canonical brand board
- `wordmark.svg` — primary wordmark
- `icon.svg` — square mark
- `palette.svg` — Deep Navy + Soft Silver palette

The Founder Console pulls tokens from `apps/web/lib/brand-tokens.ts` and CSS from
`apps/web/styles/brand.css`. Update those two files when this source changes.

## Audit footer

AI prepares · founder approves · no guaranteed claims · no auto external send
