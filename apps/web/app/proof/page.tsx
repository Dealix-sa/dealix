import { FounderShell } from "../../components/founder-shell";

export const dynamic = "force-dynamic";

export default function ProofPage() {
  return (
    <FounderShell title="Proof Center">
      <p className="lead">
        Manage approved proof, anonymized insights, case studies, and
        proof-to-demand assets. Wires in Phase 2 of the runtime binding
        plan.
      </p>
      <section className="card">No proof library connected yet.</section>
    </FounderShell>
  );
}
