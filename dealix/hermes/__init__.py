"""
Hermes Universal Kernel — Dealix Sovereign Value Control Plane.

Every input is a Signal, every Signal becomes an Opportunity, every
Opportunity becomes a Decision, every Decision an Execution, every
Execution an Outcome, every Outcome an Asset, and every Asset is
either Scaled or Killed.

The 14 non-negotiables are enforced by this package — see
docs/architecture/HERMES_UNIVERSAL_KERNEL.md for the full doctrine.
"""

from __future__ import annotations

__version__ = "1.0.0"
__all__ = ["__version__"]
